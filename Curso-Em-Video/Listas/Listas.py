"""
Variaveis Compostas listas

#Declaração:
    lista = [] -> cria uma lista vazia
    lista = list(range(x,y)) -> cria uma lista de x até y

#Comandos
    -> lista.append(x): adiciona um elemento x na ultima posição da lista.
    -> lista.insert(y,x): Adiciona um elemento x na posição y da lista.
    -> del lista(x) ou lista.pop(x): Deleta um lemento na posição x da lista.
        OBS: pop(): sem argumentos deleta o ultimo item da lista.
    -> lista.remove(x): Deleta o valor x da lista.
        -> if 'x' in lista:
            lista.remove

    ->lista.sort(): ordena os elementos da lista de forma crescente
        ->lista.sort(reverse = True): Ordena os elementos da lista de forma decrescente

    ->len(lista): retorna quantos valores tem em uma lista

    -> b = a[:] : Sendo b uma lista, o parametro a[:] cira uma copia da lista b

#Listas encadeadas
    Exemplo:
        pessoas = list()
        pessoas.append(dados[:])
"""

